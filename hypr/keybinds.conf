
# KEYBINDS
# Basic
bind = $mainMod      , RETURN                , exec                            , $term # KEEP THIS ONE PLEASE I BEG
bind = $mainMod      , BACKSPACE             , exec                            , alacritty
bind = $mainMod      , TAB                   , exec                            , $menu
bind = $mainMod      , Q                     , killactive
bind = $mainMod SHIFT, Q                     , forcekillactive
bind = $mainMod      , F                     , fullscreen
bind = $mainMod SHIFT, F                     , togglefloating
bind = $mainMod SHIFT, S                     , exec                            , $screenshot
bind = $mainMod      , E                     , exec                            , $explorer

# Lock/Sleep
bind = $mainMod CTRL , L                     , exec                            , hyprlock
bind = $mainMod CTRL , Z                     , exec                            , hyprlock & sleep 0.5 && systemctl sleep

# Media keys
bindel =             , XF86MonBrightnessUp   , exec                            , brightnessctl set +5%
bindel =             , XF86MonBrightnessDown , exec                            , brightnessctl set -5%
bindel =             , XF86AudioRaiseVolume  , exec                            , wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+
bindel =             , XF86AudioLowerVolume  , exec                            , wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindl =              , XF86AudioMute         , exec                            , wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindl =              , XF86AudioPlay         , exec                            , playerctl play-pause
bindl =              , XF86AudioPrev         , exec                            , playerctl previous
bindl =              , XF86AudioNext         , exec                            , playerctl next

# Mouse binds
bindm = $mainMod     , mouse:272             , movewindow
bindm = $mainMod     , mouse:273             , resizewindow
bindm =              , mouse:274             , resizewindow
